build
install
.zig-cache/

*.o
*.obj

/libcxx-*

/*.log

*.swp
__pycache__
/.venv/
/.direnv/

/host-tools/

# Editor and agent scratch. AGENTS.md is the only agent-facing file this repository
# carries; formatter and linter configs (.clang-format, .gdbinit, .envrc) are committed
# because the build and the debugger read them.
.vscode/
.idea/
.devcontainer/
.claude/
.opencode/
.cursor/
.cursorrules
.aider*
CLAUDE.md
GEMINI.md
