# Build trees. Every lane picks its own suffix; none of them is in-source.
build
build*32
build*64
build_deb*
build_ubuntu*
build_win*
build_doc
build_native
install
install_native
install*32
install*64
Debug
debug
gcc
clang
ssc_debian
ssc_windows
.zig-cache/
*.o

# The libc++ dependency closures built by tools/deps/, and the upstream sources they share
# (libcxx-src/). Multi-GB: the recipes are committed, the result is not. Deliberately not under
# build/ -- a clean must not discard a build measured in hours.
#
# No trailing slash: a closure shared between clones is reached by a symlink, and git matches
# a symlink as a file however directory-like it looks.
/libcxx-*

# Test droppings: run_all_tests and the tutorials write these into their cwd,
# which is the repo root. The Makefile writes .dockerignore there too.
#
# ROOT-ANCHORED ON PURPOSE. Bare **.png / **.pptx / **.h5 also match the tracked
# reference datasets under validation/ and integration_tests/ and the documentation
# assets under docs/. Git keeps those because they are already tracked, but any new
# sibling would silently vanish from `git status` -- which is how a reference dataset
# gets lost without anyone noticing.
/*.hdb
/*.HDB
/*.h5
/*.hdf5
/*.stl
/*.csv
/*.json
/*.ini
/*.png
/*.pptx
/*.log
/.dockerignore

# Generated documentation. Still blanket-matched: the pandoc/pweave lane writes next
# to its sources rather than into an output directory, so there is nothing to anchor to.
**.pdf
**.html
**.docx
**.emf
**.jpg
**.jpeg
**.bin

# Packaging
ssc.deb
xdyn.deb
*.tar.gz
*.tgz
*.bz2

# Editors, IDEs, tooling
.cproject
.pydevproject
.metadata
.project
.settings
.vagrant
*.swp
__pycache__
.wine
